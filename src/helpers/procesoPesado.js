export const procesoPesado = ( itereaciones ) => {
    for (let i = 0; i < itereaciones; i++) {
        console.log('Iteramos');
        
    }

    return `${ itereaciones } iteraciones realizadas`;
}